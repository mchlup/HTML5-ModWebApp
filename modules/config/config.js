// Základní konfigurace modulu "config".
// Používá se hlavně jako identita pro configManager.

const config = {
  id: "config",
  name: "Konfigurace",
  description: "Nastavení aplikace, databáze a modulů.",
  version: "1.2.0",
  category: "system",
};

export default config;
